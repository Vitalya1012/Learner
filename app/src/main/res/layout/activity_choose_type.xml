<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/background_color"
    tools:context=".ChooseTypeActivity">

    <ImageView
        android:id="@+id/imageViewFlag"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:adjustViewBounds="true"
        android:layout_marginTop="20sp"
        android:layout_marginStart="100sp"
        android:layout_marginEnd="100sp"
        tools:srcCompat="@drawable/flag_en"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

    <Button
        android:id="@+id/buttonModeFromRu"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="16dp"
        android:layout_margin="24dp"
        android:backgroundTint="@color/button_color"
        android:textSize="18sp"
        tools:text="from russian to english"
        app:layout_constraintTop_toBottomOf="@+id/imageViewFlag" />

    <Button
        android:id="@+id/buttonModeToRu"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="16dp"
        android:layout_margin="24dp"
        android:backgroundTint="@color/button_color"
        android:textSize="18sp"
        tools:text="from english to russian"
        app:layout_constraintTop_toBottomOf="@+id/buttonModeFromRu" />

    <Button
        android:id="@+id/buttonDictionary"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="16dp"
        android:layout_margin="24dp"
        android:backgroundTint="@color/button_color"
        android:textSize="18sp"
        android:text="@string/dictionary"
        app:layout_constraintTop_toBottomOf="@+id/buttonModeToRu" />

</androidx.constraintlayout.widget.ConstraintLayout>